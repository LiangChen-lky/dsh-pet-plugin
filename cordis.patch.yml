# dsh-pet-plugin 组合层：把两个插件行插进 web profile 的组合。
# 包名 scope 与 npm 账号 @kkkey 一致（发布时 npm 校验 scope 归属）。
- insert:
    - id: pet-assets
      name: '@kkkey/dsh-pet-assets'
    - id: ui-pet
      name: '@kkkey/dsh-client-pet-ui'
